# 当前待办

只记录尚未完成且会改变下一步工作的事项。当前项目仍处于“架构已建、正式实现未开始”。

## A. 阶段 1 — 捕获路径定型

- [ ] **全屏窗口实验。** 用户把 QQ 切换为全屏；重新检查 AX/CGWindow/ScreenCaptureKit 能否稳定定位真实 QQ 渲染窗口，比较“窗口级捕获”和“屏幕捕获+窗口几何裁剪”。不得修改 QQ、注入或访问数据库。
- [ ] **确定聊天区动态定位。** 不写死当前绝对坐标；能在 QQ 窗口尺寸变化后重新找到中间聊天正文区，并排除左侧会话列表、右侧成员区和底部输入区。
- [ ] **确定安全滚动原语。** 继续采用消息区滚轮事件，验证多页连续滚动、加载等待、无变化判断和停止行为；不使用输入框焦点，不发送按键文字。
- [ ] **确定 capture 输出契约。** 固化 OCR block 的 text/bbox/confidence/page_index/batch_id 最小结构，再开始正式 capture 模块。

## B. 阶段 2 — 消息重建与完整保存

- [ ] **实现 bbox 消息重建。** 从真实 QQ 页面验证昵称、正文、时间、多行文本、系统消息和媒体占位的几何关系。
- [ ] **实现相邻屏序列去重。** 用真实重叠页面校准阈值；禁止全局 sender+text 去重。
- [ ] **实现 batch/state。** 支持 incomplete/completed、恢复点和每群增量锚点。
- [ ] **实现规范化消息库。** 写入 `runtime/messages/messages.jsonl` 并校验 `schemas/message-record.schema.json`。
- [ ] **生成稳定分析入口。** completed batch 生成 `runtime/inbox/current.md`，其中包含本批全部待分析文字。

## C. 阶段 3 — 两群与首次全量抓取

- [ ] **安全切换两个目标群。** 先设计并验证群定位/切换，不允许盲点坐标或可能触发发送/确认的 UI 动作。
- [ ] **首次全量抓取。** 明确历史边界后抓取两个群文字；允许长任务分批恢复，但最终只把经过完整性确认的 batch 标为 completed。
- [ ] **人工抽样/程序性完整性审计。** 重点检查漏页、乱序、误去重、其它群污染，不要求用户逐条人眼核对。

## D. 阶段 4 — GPT 分析

- [ ] **确定日报输出格式。** GPT 只读 `runtime/inbox/current.md`，区分值得关注、多人共识、单人经验/传闻、争议、待核验、可能影响现有项目。
- [ ] **验证大批量上下文策略。** 如果单日文本超出一次读取/分析的合理规模，分块必须保持群、时间与讨论连续性，再做全局合并；不能先用关键词硬筛导致重要消息永久丢失。
- [ ] **保存本地报告。** 输出到 `runtime/reports/`；不自动写入魔兽项目。

## E. 阶段 5 — 每日增量与自动化

- [ ] **建立每日增量锚点。** 从上次 completed 序列锚点向新消息收敛，验证不会重复抓一整天或漏掉跨日讨论。
- [ ] **稳定后再决定是否定时。** 只有手动流程多次稳定后才考虑每天一次的调度；调度失败必须可见且不得产生伪 completed。
