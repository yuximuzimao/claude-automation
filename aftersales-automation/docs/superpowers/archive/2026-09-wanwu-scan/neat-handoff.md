# 万物巡检与平台提醒隔离收尾

归档日期：2026-09-12

本阶段完成两项工作：在售后系统内增加万物心选只读巡检；修复单元测试把“测试店铺”写入鲸灵平台提醒缓存的问题。本文只保留交接证据，当前运行规则以 `README.md`、`SKILL.md`、`docs/ops-queue.md` 和 `docs/ops-testing.md` 为准。

## 已完成

### 万物心选巡检

- 平台简称“万物”，后台名称“棒棒糖后台管理”；代码独立位于 `wanwu/`。
- 每天 07:00、15:00 进入现有 `op-queue`，与鲸灵和 ERP 操作严格串行。
- 每轮新建专用标签页；必要时等待浏览器自动填充并只点击一次“登陆”。
- 首页待办数字间隔 3 秒读取两次，完全一致才确认；“尚未参加商家考试”永久忽略。
- 成功、异常或停止后都关闭本次精确 `targetId` 并验证无残留，不关闭用户手动打开的标签页。
- 顶部状态框以绿色显示无待办，以红色显示待办或异常；非零待办和扫描异常分别创建一条汇总提醒。
- 不保存账号密码或万物平台 Session。

### 鲸灵平台提醒测试隔离

- 根因：Step14 在账号收尾阶段直接加载真实 `fetchAndCacheAlerts`，绕过测试 fixture。使用“测试店铺”的单元测试因此读取真实鲸灵页面，并污染 `data/jl-alerts-cache.json`。
- 修复：真实实现统一在 `loadDefaultDependencies()` 装配；业务流程只调用 `dependencies.fetchAndCacheAlerts`，单元测试显式提供替身。
- 第二道保护：读取和写入平台提醒前按 `accounts.json` 校验账号存在性和店铺显示名；不匹配缓存直接失效，不改名后继续沿用。
- 已删除缓存中原有“测试店铺”条目，其他真实店铺提醒保留。

## 验证

- 万物真实只读验收：未登录时单次点击登录成功，13 类待办均为 0，两次读取间隔 3 秒且一致，测试标签页关闭后无残留。
- 售后项目全量回归：483 项通过，0 失败。
- 全量测试前后 `data/jl-alerts-cache.json` 校验值一致。
- 全量测试前后浏览器原有标签页 ID 集合一致，无新增标签页或测试进程残留。
- 服务由 launchd 安全重启，健康接口正常；平台提醒接口及前端面板均不再包含“测试店铺”。

## 当前边界

- 万物只做只读待办扫描和提醒，不执行审核、发货、售后或其他平台写操作。
- 鲸灵业务判断、自动执行授权和 ERP 处理规则未因本阶段改变。
- `tasks/todo.md` 中原有“平台准备发货 + 平台无物流 + ERP 已揽收”待实施项保持不变。
