# A1 单工单完整采集验证交接

> 日期：2026-06-26  
> 状态：单工单完整采集、推理、模拟写回已验证；整账号固定清单批处理和正式入口未交付。

## 人话结论

A1 已经不只是“能打开目标工单”。在用户指定账号和工单后，系统已经跑通了单个工单从列表定位、详情 tab、采集、推理到写回原售后系统待确认语义的链路。

但这还不是完整交付。还没有验证一个账号下多张固定清单工单能否严格串行处理，也没有接正式 UI/队列入口，更没有放开真实同意或拒绝退款。

## 本次验证对象

- 账号：`14`，店铺备注 `茗瑞-KGOS`，页面店铺 `广州茗瑞商贸有限公司`
- 工单：`100001782462690101360`
- 用户授权：允许完整采集，并允许写回“待确认”或“等待重查”
- 实际写回：`simulated`，即原“待确认”语义

## 已完成

- 步骤 09 固定执行排序下拉命中，不因当前列表已经是目标排序而跳过。
- 排序下拉补齐 3 个可能文案识别，避免只找单一选项。
- 步骤 10 真实页面读取 48 小时列表并返回目标工单，工单类型读取为 `仅退款`。
- 工单类型归一化已补：`323`、`仅退款（无需退货）`、`仅退款` 都归一为 `仅退款`。
- `target-aware-collector` 在仅退款场景下不再因 `subBizType=323` 误走商品明细核对。
- 单工单完整采集、推理和模拟写回完成。

## 采集结果摘要

- 工单类型：页面详情原始类型 `323`，归一后为 `仅退款`
- 售后原因：`多拍/拍错/不想要`
- 子订单数：2
- 赠品数：1
- ERP 搜索次数：2
- ERP 物流读取次数：3
- 鲸灵物流包裹数：2
- 采集降级项：
  - `product-detail: 跳过（工单类型=仅退款，无需核对商品明细）`
  - `erp-aftersale: 无退货快递单号，跳过`

## 推理与写回

- simulation id：`manual-collect-1782470482079-100001782462690101360`
- queue item：`q-1782467444659-1167`
- queue status：`simulated`
- decision action：`escalate`
- 规则：`flow-5.3` 的赠品检查
- 结论：主品已退回，但赠品 `YT7629306404466` 在途未拦截成功，需人工确认

用户随后手动删除了页面中的该工单 active 推理采集信息；当前 `queue.json` 已无该条 active item，但 `data/simulations.jsonl` 保留历史 simulation。前端主列表按 queue item 展示，所以删除后页面不再显示该条 active 记录。

## 与原计划差异

原计划截至 2026-06-25 只要求最小浏览器回归：打开账号、准备列表、定位目标工单、点击处理、锁定新详情 tab、关闭详情 tab、复核列表 tab，并且不采集。

实际 2026-06-26 因用户追加授权，范围扩大为完整采集和模拟写回。这验证了更深一层链路，但不能直接等价为固定清单批处理可交付。

## 下一步

1. 用用户指定账号跑“关闭自动执行”的最小固定清单批次。
2. 验证同一账号内多张工单按首次排序清单严格串行处理。
3. 每张工单验证：列表定位 → 打开详情 tab → 采集推理 → 写回 queue/simulation → 关闭详情 tab → 回到列表继续下一单。
4. 账号收尾验证：只保留当前账号售后列表主 tab，不动 ERP、售后系统和其他非鲸灵 tab。
5. 最小批次通过后，再做规格审查和代码质量审查。

## 禁止事项

- 不得接正式 UI/队列入口。
- 不得重启加载正式入口。
- 不得真实 approve/reject。
- 不得因为单工单采集成功就放开自动执行。
- 不得把步骤 14 做成独立结果系统；必须继续写回原 `queue.json`、`simulations.jsonl` 和原三标签页语义。

## 验证证据

- 真实页面单工单链路：账号 14 茗瑞、工单 `100001782462690101360` 已完成。
- 全量测试：2026-06-26 `npm test` 204/204 通过。
- 相关测试覆盖：排序下拉 3 文案、售后类型归一化、仅退款 target-aware 采集路径。
