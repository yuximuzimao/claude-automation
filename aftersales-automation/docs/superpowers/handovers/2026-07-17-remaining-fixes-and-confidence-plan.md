# 剩余修复与新置信方案恢复登记

状态：2026-07-17 已确认并持久化。本文件是清空会话上下文后的唯一恢复入口。

## 已完成的第一个问题

ERP 全行核验、平台交易号验单、搜索最多两次、退回待确认假信号、多包裹和全部赠品子订单采集已经完成。最终规则、验证和提交见：

- `docs/superpowers/handovers/2026-07-17-refund-only-erp-logistics-handoff.md`
- `84f43e9`、`b7dce8a`、`c0f20a1`

## 接下来先处理的两个问题

两个问题都必须先用人话重新对齐，再做最小修正；不能借机改造置信系统。

### 问题二：无退货单号的业务语义

- 场景：七天无理由退货退款，但客户还没有退货单号。
- 已确认含义：客户刚发起退货申请，正在等待商家同意退货，不代表客户超期未退。
- 目标边界：固定人工处理申请；不能执行 `OVERDUE_RETURN` 拒绝，也不能计入“已退回且精确匹配”的自动分支。
- 下一步：先核对现有代码到底在哪个条件下把它判成超期，再向用户说明原逻辑和最小改法；得到确认后才改。

### 问题三：证明是真实多退，而不是采集或推理错误

- 当前风险：ERP 页面可能残留已展开的非收货行，推理可能把这些商品一起累计，形成假多退。
- 最低证据：只统计明确已收到退货的行；非收货行带商品明细、重复 ERP 售后单、跨工单复用退货记录或关键字段冲突时直接转人工。
- 商品核对：主品和赠品分别保留业务边界；按各子订单自己的数量和 `specCode` 汇总；所有应退规格已退足、次品为 0、无未知规格，且至少一个已知规格确实多退，才算真实多退。
- 当前事实：截至 2026-07-16 的一个月历史窗口内没有可信的真实多退样本，因此该分支从 0 重新累计，不能继承旧 surplus 结论。
- 下一步：问题二完成后，再拿实际页面和现有代码逐项对齐；没有现实证据的问题不盲目扩改。

## 新置信方案：已经定稿，只是暂缓

### 唯一方案来源

- 分支：`codex/aftersales-confidence-safety-v1`
- 方案提交：`9968cd8 docs(confidence-v2): replace overdesigned plan with branch registry`
- 方案文件：`aftersales-automation/docs/superpowers/specs/2026-07-16-after-sales-branch-automation-design.md`
- 当前工作树：`/Users/chat/.config/superpowers/worktrees/claude/aftersales-confidence-safety-v1`

### 必须遵守的恢复规则

1. 新方案已经写好并得到用户确认，目前只是为了先修现有问题而暂停，不需要再次重新设计。
2. 新方案推翻旧 confidence-v2 的自动学习、复杂证据状态机和通用置信指纹思路。
3. 旧分支已经实现的 Task 1–6 代码不作为后续实现基础，默认废弃；未提交的 Task 7 `fingerprint.js` 及测试明确不继续。
4. 恢复开发时不能整分支合并，也不能因为旧代码已经做了一半就迁就旧结构。应以最新主干为代码基础，只把上述新方案当需求来源，按新方案最小落地。
5. 若旧实现中个别代码恰好直接满足新方案，也必须逐项证明后才可最小复用；复用不等于继续旧计划。

### 新方案的人话口径

- 不自动学习，不自动生成规则，不自动开启处理。
- 先按售后原因分类，再按工单类型和最小最终结果细分；不同原因、不同结果分别累计。
- 列出全部现有业务分支；新组合或信息不完整时默认人工。
- 每个最小分支分别显示工单数、好评、差评、自动成功、人工处理和备注分布。
- 达到阈值只显示“可审核”，必须由用户手动启用。
- 不开放自动拒绝；主品和赠品继续分开保存与展示。
- 初始只允许用户已确认稳定的“七天无理由退货退款、ERP 已收货、主品赠品逐规格精确退回、无次品、无未匹配、数据完整无冲突 → 同意退款”分支。

### 数量口径提醒

方案文件写入时的静态历史快照是 28 个不同工单、24 次好评、26 次可关联自动成功；用户随后确认该稳定场景实际已重复执行 61 次且有多次成功自动执行记录。恢复开发时必须重新跑当前历史拆解，以最新去重数据为准，不能把旧快照或“61”直接硬编码。

## 固定恢复顺序

1. 读本文件和第一个问题的交接文档。
2. 只讨论并修复问题二，验证后归档。
3. 再讨论并修复问题三，验证后归档。
4. 两个问题完成后，读取 `9968cd8` 的新方案，从最新主干开始实现；不要继续旧 confidence-v2 半成品。
5. 先产出只读历史分支清单供用户核对，再接自动门禁；未经用户明确授权，不执行真实退款或扩大自动范围。
