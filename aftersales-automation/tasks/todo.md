# 待处理优化项

## [ ] Phase 2：无痕浏览器架构
- 新增 `lib/browser.js`（无痕上下文创建/注入/关闭）
- 修改 `scan-all.js` / `op-queue.js` / `cli.js` / `sessions/jl.js`
- 目的：自动化脚本和用户手动浏览完全隔离，互不干扰
- 背景：本 session 完成了 Phase 1（刹车）+ Phase 3（刷新优化），Phase 2 因 Phase 3 紧急问题暂跳

## [ ] 重复归档 bug（工单 100001777020400849821）
- 同一工单被自动处理归档一次、点评价后又归档一次 → 历史记录出现两条
- 第一次自动归档时会抓取后一次的评价数据，导致无评价的工单默认显示好评
- 需要排查归档触发逻辑，防止同一 queueItemId 重复写入 simulations.jsonl

## [ ] 重新采集后差评丢失（工单 100001777101872774837）
- 工单已差评，重新采集后历史记录里差评消失（统计复盘里仍在）
- 根因：重新采集覆盖了 simulation 记录，但没有保留原有的 feedbackStatus / groundTruth
- 需要重新采集时 merge 已有评价字段，不能整条覆盖

## [ ] ERP 发货行数量解析（flow-5.3 数量验证）
- ERP textSnippet 里有每行发货数量（如 `10 (2种)`），目前未解析
- 正确逻辑：各发货行数量之和 ÷ subItemNum = 实际发货套数，与 afterSaleNum 比较
- 需改：`lib/erp/search.js` READ_ROWS_JS 解析 qty 字段
- 注意：textSnippet 里的 `10 (2种)` 中 10 是商品总件数，不是套数；套数 = qty ÷ subItemNum

## [ ] 物流采集完整性判断修正
- 当前逻辑：鲸灵物流包裹数 vs ERP 发货行数 比较，但两者对应的可能不是同一个子订单
- 已知问题：工单有多条 subOrders 时，鲸灵物流展示的可能是另一条子订单的包裹
- 待重新设计：如何可靠判断物流是否"采集完整"

## [ ] RULES.md 渐进式披露重构
- 按工单类型拆分章节为独立文件或加行号锚点
- Session 开始只读 0.1-0.3（约500 tokens）
- 按工单类型按需加载对应章节
- 前置条件：当前规则已稳定
