# AGENTS 与 Skills 审阅方案（修订版）

日期：2026-09-06。状态：**已确认、实施并完成 NEAT 归档。**

本文件保留当时的候选与讨论记录，其中“待确认”等措辞只反映审阅阶段，不代表当前状态。获批范围、Claude Code PUA 移除和 web-access 元数据追加修复均已完成；最终结论与验证摘要以同目录 `README.md` 为准，不要重复实施。

原活跃路径为 `docs/agents-skills-review-plan.md`，现已迁入本历史目录。本文不是现役指令，当前行为必须读取实际 AGENTS / Skills。

## 结论与边界

建议 A1、A4、A5、A6、A8 修改后保留；A2、A3 合并为一条，删除分开落地的做法。A7 的正文优化不再推进；建议不安装 grill me，将 PUA 移除列为待确认事项。三方认可仍待用户和 GPT 审阅本修订稿；此前“倾向认可”不等于批准这次的新文本。

- `khazix-writer`、`hv-analysis`、`aihot` 是备用技能，冻结方法、触发、创作边界、作者定位、交付规格和来源范围。只允许另行获批的官方更新或纯技术兼容修复；本轮不更新。
- 保留用户对写作、设计和业务的决定权，保留适合 Astra 与 Sol 的必要检查。判断依据是规则的实际作用，不按模型名称猜测哪些纪律可以删除。
- 只审 A1/A2/A3/A4/A5/A6/A8 和是否替换 PUA。上一版“暂不建议实施的方向”不在本轮展开；不调整 NEAT、中文风格或其他 Skills。
- 同一处优先替换旧句，避免追加解释层。例子与论证放本文，未来生效文件只放短规则。

## A1：确认现有规则与批准规则变更

**问题与场景：** `/Users/chat/AGENTS.md:19` 的“新项目或新目录优先确认规则”可以理解为读取约定，也可以理解为每次向用户确认；后半句“先更新说明文档，再改实践”只规定顺序，没有表达由谁批准规则改变。进入已有项目时不应重复问已有答案，但修改项目约定仍由用户决定。

**落点与频率：** `/Users/chat/AGENTS.md` 的“工作方式”第一条，原位替换。属于高频用户级上下文。不是修改 `/Users/chat/claude/AGENTS.md`，也不改变项目自己的新建流程。

**长度与重叠：** 原候选列举目标、结构、安全、习惯等取舍过长。A1 只管读取和改变规则；批准的一般语义交给 A2/A3，不重复列例外。

**推荐最短文本：**

> 进入新项目或目录先读取结构、命名、数据、清理与安全约定；需改变项目规则时，先提交方案，经用户确认后更新说明并实施。

**建议：修改后保留。** 比原候选短，保留原来的检查对象和文档先行要求；不会把普通目录读取升级成批准流程。

## A2 / A3：合并授权范围与完成判据

我认可 GPT 的合并方向，但不直接采用“批准的是阶段，不是每一个机械动作”。这句话若脱离上下文，容易让模型把退款、提交等动作自行包成一个阶段。用户批准的内容才定义阶段，原有业务逐步批准不能被覆盖。

| 候选 | 具体问题与生效场景 | 现有依据 | 长度、重叠与结论 |
|---|---|---|---|
| A2：范围内执行、边界处批准 | 已批准本地修复后，不必每次读文件、检查或修自身引入的问题都再问；但不能据此改其他模块、改设计方向或继续下一业务操作 | `/Users/chat/AGENTS.md:22` 要求直接修 bug，`:60` 要求系统承担复杂性，`:66` 要求业务逐步等待。它们可以共存，欠缺的是一句适用范围说明。`:15` 已要求先查上下文，故不再增加“先查后问”的重复规则 | 原候选把查信息、决定权、重复批准和下一步都展开，过长。**修改后保留，合入 A3，不单独新增段落** |
| A3：当前阶段何时完成 | 只批规划时交付计划；批实施时不能只说“已启动/已派工”，而要交当前阶段的结果和验证。真实阻塞可如实交接 | `/Users/chat/AGENTS.md:23,25` 已有验证与读回，但没有总的阶段完成判据；`:79` 及工作区 `AGENTS.md` 的“审查和修复分阶段”说明阶段边界已有实际用途 | 原候选例子可留在方案，不必全塞全局。它与 A2 共用“批准范围”，**修改后保留，删除独立落地形式** |

**共同落点：** `/Users/chat/AGENTS.md`“工作方式”中紧接 bug report 条目，新增一条；不在“自动化和外部操作安全”再复制，也不改其逐步批准原文。高频上下文只承担这一条跨任务原则。

**推荐合并文本：**

> 在用户已批准范围内连续完成必要工作及相关验证；扩大范围、改变约定方向或涉及用户保留的决定权时先获批。阶段以用户授权为准：只批规划则交计划，批实施则交修改和验证结果；启动、派工不等于完成，受阻如实说明。业务逐步确认及专门限制照旧。

**为何不再缩成口号：** 需要同时保留授权来源、范围外批准、规划/实施交付、真实阻塞和业务例外；再缩短会把歧义重新带回来。该文本不要求小任务先建阶段表，也不要求重新批准已有授权。

**具体例子（仅帮助审阅，不写进 AGENTS）：** 改魔兽页面间距并检查布局可在当前请求内完成；认为整页重做更好，要另问。批准列退款方案只能交方案，不能退款。业务操作遇到风控仍按原规则停止；“连续完成”不授权重试。验证失败但能在原范围修复时继续处理，缺少必要人工输入时如实交接。

## A4：把技术兼容与流程变化分开批准

**问题：** `hv-analysis/SKILL.md:55` 固定 `/mnt/.claude/...`，`:53–59` 固定 WebSearch/WebFetch 等工具名，`:22,212` 要求 `pip ... --break-system-packages`。`web-access/SKILL.md:20` 依赖 Claude 专属变量，`:17` 要求所有联网先检查 CDP，但“联网工具选择”又提供公开搜索、读取、curl 与 CDP 的不同路线。

**落点与频率：** 只修改 `/Users/chat/.codex/skills/hv-analysis/SKILL.md` 的工具说明与两处依赖安装说明，以及 `/Users/chat/.codex/skills/web-access/SKILL.md` 的“前置检查/联网工具选择”；CDP 参考有同一旧路径时同步 `/Users/chat/.codex/skills/web-access/references/cdp-api.md` 的启动示例。均为按需正文，不进入全局 AGENTS，不增加 description。其余两个备用技能没有本轮已定位的必要兼容补丁，不改。

**A4a：纯兼容修复，修改后保留。** 原候选是宽泛原则，容易给执行器过大范围。改为只对应以上已定位位置，以下短文分别替换其旧说明，不新增全局 adapter 架构：

> 工具名按当前环境的等价能力映射；Skill 路径按实际安装位置解析。只调整调用方式，不改变来源要求、授权边界或交付标准。

> 依赖已满足则复用；缺失时在隔离虚拟环境安装，并用该环境的 Python 运行转换脚本，不改系统 Python。

保留原 PDF 产物、字数、署名、研究方法和 arXiv 来源要求。未提供等价能力时报告该步骤缺口，不能以“降级”为由替换必需来源或省略 PDF。

**A4b：CDP 前置检查条件，单独确认后保留。** 推荐把前置第一句替换为：

> 仅进入浏览器 CDP 路径时执行以下前置检查；公开搜索、网页读取和 curl 路径不依赖 CDP。需要浏览器时再检查，后续登录与授权要求不变。

这确实改变原来的执行顺序，不应把它伪装成纯路径修复。它与该 Skill 自己的多路线设计一致，可以避免尚未使用的浏览器阻断公开读取；但用户应单独确认这一流程调整。不要连带移除浏览器选择、风险提示、登录或持久配置规则。

**重叠处理：** A4 负责如何到达工具；A6 负责已涉及浏览器/代理时的归属与共享状态。都改 web-access，但替换不同原句，不复制相同边界。A4a 的跨平台说明由两个相关 Skill 就地维护，不给全部备用技能批量加前言。

## A5：被扫描对象只读

**问题：** `/Users/chat/.codex/skills/storage-analyzer/SKILL.md:23` 禁止“任何写操作”，但后续步骤要求写扫描 JSON 与 HTML。按字面执行会阻断本 Skill 自己的产物。

**落点与频率：** 只替换该文件“铁律”第一条；按需正文，不进入全局，不改第二条删除确认，也不改整机扫描、报告形式和服务默认行为。

**推荐最短文本：**

> 分析阶段对被扫描数据只读；允许在约定输出位置写入扫描结果、分析 JSON 和报告，并读回核对。被扫描数据的修改、移动、删除仍按原确认规则执行。

**长度与重叠：** 原候选已较短，本版只让“分析阶段”和输出例外更明确；与 A2/A3 无须再合并，属于扫描专用边界。

**建议：修改后保留。** 这是有直接证据的文字冲突，不涉及转移删除决定权。

## A6：进程归属与共享浏览器状态

**问题：** `web-access/SKILL.md:32` 与 `references/cdp-api.md:8` 使用全局 `pkill -f cdp-proxy.mjs`，可能误停用户或其他任务的代理；`SKILL.md:208` 把不同 targetId 等同于“无竞态风险”。后者没有区分独立 DOM 与共享账号、存储及服务器业务状态。

**落点与频率：** `/Users/chat/.codex/skills/web-access/SKILL.md` 中的切换浏览器句、任务结束句和并行 CDP 句；`references/cdp-api.md` 中同一进程说明同步替换。均为按需正文。全局 AGENTS 已要求进程归属和测试零残留，不另加同义段落。

**推荐最短文本（替换不同位置，非一段新总则）：**

> 停止代理前确认精确 PID、归属和使用状态；只处理本任务有权停止的进程，不用全局 pkill，不中断用户或其他任务的代理。

> 既有用户代理保持不动；本次测试产生的临时代理按工作区规则清理并复查，不因“建议常驻”豁免测试清理。

> 不同 tab 只隔离各自 DOM，不保证共享状态无竞态；登录、切换账号及涉及共享会话或业务状态的动作仍按项目规则串行。

这里“共享状态”不表示所有 sessionStorage 都跨 tab 共享；只强调 tab 分离不是完整隔离证明。普通独立读取可按现有并行规则处理。

**长度与重叠：** 原候选两层方向正确；各短句就地替换旧说法，避免重复写一套测试清理流程。A4b 不启动无关 CDP 可以减少触发 A6 的次数，但不能代替 A6。

**建议：修改后保留。** 这是落实已有控制边界，不扩大停止服务的授权；若代理不属于本任务且确需重启，仍要先确认。

## A8：设计方法辅助用户方向，不接管局部维护

**问题：** `/Users/chat/.codex/skills/frontend-design/SKILL.md:3` 的 reshaping 容易覆盖小改动；`:9` 默认客户已拒绝模板，要求承担美学风险；“Process”规定完整设计步骤。用于魔兽页面的间距、阅读顺序、字体等明确修改时，可能误启动整体视觉探索。

**角色判断：** 本 Skill 是新设计或明确获批重设计时的辅助方法。普通 UI 维护以用户明确需求与既有视觉体系为准，不默认启动整套设计流程。即使是新设计，也应落实用户给定方向。

**落点与频率：** 仅 `/Users/chat/.codex/skills/frontend-design/SKILL.md` 的 description 和正文开头。description 是高频技能发现摘要，正文按需加载；不在全局 AGENTS 追加 UI 专项规则，不改原设计工具箱。

**推荐 description：**

> 用于新界面或用户明确同意的整体重设计，辅助落实用户方向。局部 UI 维护不启动完整设计流程。

**推荐正文入口：**

> 局部维护按用户明确需求修改，并保持现有视觉体系。认为整体重设计更合适时，先说明理由并询问；未获同意，继续完成原定局部修改。下述设计流程仅用于新设计或已获同意的整体重设计，以用户方向为准。

**长度与重叠：** 比原 A8 更明确。description 负责发现，入口负责防误触发，属于必要的最短摘要与执行边界；后文不用重复。A2/A3 管通用范围，A8 解决视觉 Skill 特有的流程误用，因此保留在本 Skill。

**建议：修改后保留。** 不能因为发现重设计机会就暂停原本可以完成的局部修改；“先问”只约束额外的整体重设计。若局部要求本身无法满足，应说明具体限制，不能私自升级。

## 全局文字预算与适用范围

建议只在 `/Users/chat/AGENTS.md` 替换 A1 并新增合并 A2/A3 一条；其余候选全部就地修改 Skill。合并规则是 114 个字符（含标点，非 token 估算）。不另建模型专用规则、规则路由系统或每次任务的阶段表。

`/Users/chat/AGENTS.md` 是用户级入口；`/Users/chat/claude/AGENTS.md` 是工作区入口，两者不是同一个文件。现有下层工作区的业务批准、冻结已验证资产、风控停止和审查/修复分阶段继续有效。

ChatGPT 通过 CodexPro 能访问文件，不足以证明它自动加载用户级 AGENTS。GPT 执行或审阅时应由交接明确指向适用文件；先核对加载入口，暂不为假设中的加载差异在多个文件复制同一规则。

## PUA 与 grill me：建议两者都不纳入常规工作流

**更新判断：支持用户倾向，不安装 grill me，将 PUA 移除列为待确认事项。** 这不是批准现在删除；本轮仍只修订方案。A7 的 PUA 正文优化不再推进，也不为移除它另造一套替代技能。

### 新证据如何改变原建议

此前建议暂留 PUA，是因为把“目前用着顺手”视为主要证据，并担心失去执行纪律。用户补充：这段时间 GPT 经 CodexPro 工作时并不使用 PUA，实际效果仍满意；用户也习惯阅读模型展示的分析，及时指出问题和补充想法。已有持续使用反馈，因此没有必要再强制安排对照实验，才能决定是否保留一个个人辅助技能。

这足以支持“在用户当前任务和协作方式下，PUA 的额外价值不明显”。它不证明 5.6 Sol-high 在所有任务里都不需要排障约束，也不要求先证明这个更大的命题才能做个人工具取舍。

对 grill me，用户看重它用简短直接的追问挖出难以表达的重要需求；但用户已经在自行梳理、阅读模型分析和补充反馈，并要求有疑问就问。专用访谈 Skill 目前没有明确填补一个缺失环节。保留其思路不等于必须安装它，尤其不需要把自然对话固定成额外流程。

### 建议保留什么

- 保留当前 AGENTS 和项目规则中的相关验证、读回、业务批准及范围边界；不因移除 PUA 把这些独立规则一并删掉。
- 对会影响目标、正确性或用户决定权的疑问，模型具体提出并解释影响；已能查明的事实先查。继续使用现有沟通约定，不新增一份长问询清单。
- 用户通过可见分析发现问题，是当前协作中的有效反馈渠道；完成结果仍用文件、实际状态和必要检查核验。

### 待确认的实施范围

若用户之后明确批准移除 PUA，先定位实际生效的安装入口、自动加载配置及直接引用，再给出最小移除清单；保留已有恢复副本。仅在获批平台和路径内实施，不能顺手删除其他平台副本、无关配置或历史资料。删除目录与关闭默认加载不是同一件事，应在实施清单里区分，避免留下失效入口。

grill me 不安装，无须继续追索版本或做安装适配。之前核查的 [Matt Pocock 原入口](https://github.com/mattpocock/skills/blob/main/skills/productivity/grill-me/SKILL.md) 与 [grilling 方法](https://github.com/mattpocock/skills/blob/main/skills/productivity/grilling/SKILL.md) 仅是比较依据，不构成后续任务或推荐。

**当前状态：PUA 文件、开关和引用均未改变，grill me 未安装；建议方向已改为减少不必要的技能，而非用一种替换另一种。**

## 交给 GPT 的下一步

1. 只审阅本修订稿，尤其是 A2/A3 的业务例外、A4a 与 A4b 的区分、A8 是否准确表达用户主导权，以及“不安装 grill me、PUA 待确认移除”的更新建议。
2. 用候选编号记录三方最终意见。A4a 与 A4b 可分别决定；“倾向认可”“方案看起来合理”不自动视为执行批准。
3. 用户明确批准实施后，才生成对应真实文件的最小补丁。对备用技能只允许已核实的兼容点；不得应用上一轮已撤回的整文件补丁。
4. 读回、检查差异、运行适用的技能结构校验；无需为验证文字规则启动浏览器、业务写入或后台任务。

原轮次 22 个文件的改动已回退；28 个备份条目当时逐一验证一致。历史回退证据仍在 `/Users/chat/.codex/state/instruction-audit/20260905-200213/`，仅供必要追溯，不是 GPT 读取本方案的前置条件。本轮交接结果见 `.ai-bridge/agent-status.md`；其 diff 只涉及审阅方案，不是 AGENTS/Skills 的实施补丁。
