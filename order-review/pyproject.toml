[project]
name = "order-review"
version = "0.1.0"
description = "Read-only ERP order review floating window"
requires-python = ">=3.13"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
