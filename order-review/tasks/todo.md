# 审单待办

授权和当前状态见 `docs/CURRENT.md`。装箱页面/资料/独立本地服务已迁回主干；正式浮窗仍未接入，任何真实ERP操作仍需按原授权规则逐单确认。

## 装箱实验主线

- [x] 算法研究：明确采用完整 `1～N箱外层 + 单箱几何内核`，单箱只是 `N=1` 特例；研究记录见 `docs/2026-09-12-packing-algorithm-research.md`。
- [x] 算法实现第一步：保留现有严格 verifier，把物件顺序纳入搜索；已加入少量确定性排序、等价类/旋转去重与共享 deadline，并用“薄礼袋先铺底”反例闭环。
- [x] 支撑模型拆层：几何有效性只检查箱内边界/不重叠；离地物件至少要有实际承重点，完整底面支撑改为独立稳定性指标。搜索优先完整支撑方案，只在当前预算内找不到时才返回部分支撑方案，并输出最低支撑覆盖率供实物复核。
- [x] 算法实现第二步：候选点已独立成混合 frontier / Extreme-Point 风格层，优先低层、高支撑、直接边界点，同时保留旧坐标笛卡尔组合作兜底；增加同类物件规则网格 fast path，并修正自动选箱预算分配，避免一个小箱耗尽整单节点预算。
- [ ] 算法实现第三步：建立 `pack_order()` 1～N箱外层搜索；盲算不读取历史保存方案、历史人工拆法或固定容量答案，历史仅在完成后对照。
- [ ] 历史盲测（完整1～N仍待做）：2026-09-13单包裹快照曾有146/148找到摆法，2种因当时酵素4.0原箱缺尺寸保持UNKNOWN。2026-09-17已补原箱尺寸并增加多项正式原箱，该快照不再代表当前资料；实现 `pack_order()` 后重新统计完整可行率、包裹数、最优性、UNKNOWN、耗时和人工差异。
- [x] 页面/资料集成：独立本地服务、静态页面、固定3466入口、商品身份链和商品尺寸拆表已迁回主干；页面当前只开放KGOS单箱是能力边界，不限制完整1～N箱算法架构。
- [x] **主干页面实测验收**：用户已在 main 的3466页面完成首轮实测；发现三视角Canvas实际已绘制但被空状态遮罩覆盖，已用全局 `[hidden]` 规则修复并现场确认可见。用户确认当前页面剩余关注点转为算法本身；后续发现具体UI问题仍只改受影响处。
- [x] **清理历史 worktree**：`order-review-packing-simulator` 的业务内容已全部迁移或明确丢弃，用户页面验收后已删除本地 worktree；远端旧分支仅作历史备份，不再作为开发入口，也不得回抄业务数据或规则。
- [ ] P4悦希：商品匹配完成后，按固定组合明细逐条对应原始活动笔记；保持原文未解析状态直到确认。

## 待补资料或业务确认

- [x] 新版咖啡共同单盒尺寸与80盒原箱尺寸：两款单盒均为 `90×37×154mm`，原箱为 `393×380×328mm`，两款编码、等尺寸及混装均已确认；规则排列 `10×4×2` 可容纳80盒。
- [ ] 咖啡约70盒原箱下限、修颜额外2/4个面霜容量仍未实测确认。
- [ ] “胶原饮箱”对应哪个箱型，以及活动组合/额外单品关系，等待用户解释。
- [ ] 其余缺尺寸商品按资料目录逐项补齐；礼盒制作尺寸已收到，礼袋5mm厚度仍是估计。

## 装箱实验交互待办

- [ ] 添加商品后保留当前已选商品和搜索内容，不再调用 `clearProductSearch()` 立即清空。用户可连续为同一商品追加数量；只有切换品牌、清空本次商品或主动选择其他商品时才改变当前选择。
- [ ] 本次商品列表为每一行提供可随时修改的数量输入。修改后必须校验正整数、更新同商品合并数量、使旧装箱结果失效并刷新可选原箱；不要求删除商品后重新搜索添加。
- 验收：连续添加同一商品无需重新选择；任意商品数量可直接改大或改小；非法数量不写入；数量变化后运行按钮、纸箱候选和后续计算全部使用新数量。

## 暂停能力待重构：拆分后的核验与审核

- [ ] 重新设计“点击拆分二次确认”之后的状态模型。目前浮窗在该点击完成后立即停止，只提示人工回ERP核对；不读取拆分结果、不核验逐包明细、不打开或提交审核、不自动刷新下一单，并阻止当前订单重复提交。
- [ ] 重构结果核验时，重新验证至少3个结果的单向读取与滚动，以及4个以上结果的虚拟列表卸载；不得直接恢复旧后半段代码。
- [ ] 重构拆分后审核时，单独证明结果订单身份、勾选范围、审核弹窗数量、提交结果和幂等边界。只有自动测试与真实订单验收都通过后，才能重新开放。
- 自动化覆盖不能代替真实实测；不为NEAT执行真实订单。历史证据只作参考，见 `docs/archive/2026-08-28-split-single-pass-verification/neat-handoff.md`。

## 现役优化待办：优化“保存方案”的界面等待

- 现象：在审单悬浮窗点击“保存方案”后，界面会等待数秒才显示成功。
- 当前原因：`ui._confirm_package_plan()` 在 Tk 主线程内同步调用 `PackagePlanWorkflow.confirm()`。新案例写入需依次完成文件锁、整份状态流式序列化与 `fsync`、写前隔离进程校验、有效旧版备份、原子替换、写后隔离进程校验和未使用内存回收；这些安全步骤全部阻塞 UI 事件循环。
- 下次先做分阶段耗时记录（等锁、序列化/`fsync`、写前校验、备份、写后校验、内存回收），用真实案例文件确认主要耗时，不凭感觉删减安全门禁。
- 目标方案：把完整的 repository `confirm` 持久化移到单一后台 worker；Tk 主线程立即显示“正在保存…”并禁用重复保存、刷新和修改当前草稿。后台完成后只通过 Tk 主线程回写成功/失败状态；成功前不得清空草稿或显示“已保存”，失败时必须保留草稿以便人工重试。
- 安全边界：保留现有跨进程锁、写前/写后校验、有效备份、原子替换和失败回滚；不为提速把校验改成 best-effort。保存中关闭窗口、切换订单和连续点击必须有明确且可测试的单次任务语义。
- 验收：保存期间悬浮窗仍能绘制“正在保存…”状态；双击只产生一条案例；成功后独立读回验证；写入/校验失败保留草稿且不损坏原案例；测试前后零新增 pytest、Tk 窗口和 worker 线程残留。
