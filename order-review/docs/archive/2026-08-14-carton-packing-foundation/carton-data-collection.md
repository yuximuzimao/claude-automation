# 纸箱与商品尺寸数据采集清单

## 目标

这批数据用于判断“哪些商品能放进哪个实际可用纸箱”，进而比较包裹数，而不是
建立一套看起来精确、实际无法落地的理论体积模型。数据可以分批提供；精确值、
估算值和未知值必须分开标记。

用户可以直接提供 Excel、CSV、照片、现有台账或文字记录。下一会话先整理和审计，
不要求用户预先转换成程序格式。

## 第一优先级

### 1. 纸箱

每种实际可用箱型尽量记录：

| 字段 | 要求 |
|---|---|
| 箱型编号或常用名称 | 能稳定区分不同纸箱即可 |
| 内长、内宽、内高 | 推荐毫米；优先量可实际使用的内部空间 |
| 尺寸类型 | `内尺寸`、`外尺寸` 或 `未知`；当前外尺寸按三轴各减 `5 mm` 换算 |
| 是否仍在使用 | 停用箱型不能作为线上候选 |
| 备注 | 折边、内衬、明显不可用空间等实际限制 |

只有外尺寸也可以先交付，并保留“外尺寸”标签；当前已确认的换算规则是长、宽、
高各减 `5 mm`，不是未经确认的纸板厚度猜测。

### 2. 商品

按最终发货时的单件包装记录，不量裸产品：

| 字段 | 要求 |
|---|---|
| 商家编码 | 优先使用 ERP 稳定编码 |
| 商品标准名称 | 便于人工核对 |
| 包装长、宽、高 | 推荐毫米，包含外盒、袋装鼓起等实际占用 |
| 尺寸精度 | `实测`、`供应商数据`、`估算` 或 `未知` |
| 摆放限制 | 可自由旋转、可侧放、必须直立、不可叠压或未知 |
| 备注 | 易压、形状不规则、多件会嵌套等情况 |

同名商品如果包装规格不同，必须拆成不同记录；口味相同体积白名单仍只按项目
根本规则使用，不能因为名字相近自动合并。

## 同样有价值的不完整证据

### 3. 已知装箱案例

即使不知道纸箱，也请记录真实发生过的组合：

- 包含哪些商品、各多少件。
- 结果是确定能装、确定不能装，还是只知道最终用了几个包裹。
- 如果知道箱型就填写；不知道则明确写“箱型未知”。
- 证据来源标记为实际打包、人工经验、历史系统记录或估算。

“确定能装但箱型未知”可以证明组合可行，不能证明任何一个已知箱型可行；算法
必须保留这层区别。

### 4. 大概容量

例如“这个箱大约放 6～7 盒”“20 盒箱通常能装 18 盒”都可以记录：

- 对应箱型（不知道可留空）。
- 商品或商品组合。
- 大概数量或范围。
- 是实际装过、长期经验还是目测估计。

这类数据用于缩小候选和安排人工复核，不直接当作绝对容量上限。

## 推荐表格结构

如果准备 Excel，四张表最清楚：

1. `纸箱`：箱型编号、名称、长、宽、高、尺寸类型、是否在用、备注。
2. `商品`：商家编码、名称、长、宽、高、尺寸精度、摆放限制、备注。
3. `装箱案例`：案例编号、箱型编号（可空）、结果、证据来源、备注。
4. `案例商品`：案例编号、商家编码、数量。

案例商品单独成表，可以表达一个箱子里混装多种商品，不需要把组合塞进一个单元格。

## 第一版可以暂缺

- 商品重量和整箱重量。
- 纸箱承重。
- 缓冲材料的精确厚度。
- 纸箱成本和快递计费体积。
- 所有商品、所有箱型一次性全覆盖。

这些字段以后可以扩展，但当前不应阻塞尺寸模型与算法可行性判断。第一轮更重要的
是确认：内尺寸是否可信、商品包装尺寸覆盖多少真实订单、旋转限制是否明确，以及
未知箱型的历史可行证据能覆盖哪些组合。

## 2026-08-13 已收到数据

- 咖啡正装最终包装：`105 × 42 × 154 mm`，来源为产品包装设计图。根本规则已
  确认的两个咖啡正装口味共用该规格。
- 益生菌正装最终包装：`115 × 28 × 93 mm`，商家编码 `6977987940053`，来源为
  产品包装设计图。
- 现有 12 种纸箱只提供了外尺寸。用户确认统一按长、宽、高各减 `5 mm` 得到
  保守可用内径；按该内径完成三维摆放即可作为确定能装的证据。
- 咖啡原箱 60 盒，外尺寸 `445 × 332 × 328 mm`；仅允许美式和生椰正装共用，
  不与其他品类混装。
- 酵素4.0单盒 `171 × 151 × 73 mm`，原箱最大 20 盒但原箱尺寸未知；用户确认
  15～20盒均可直接使用原箱，最低可发数量为15盒。
- 糖果与糖果2.0单盒 `101 × 36 × 130 mm`；当前只确认糖果2.0商家编码
  `6976299500108`，旧糖果编码待补。
- 益生菌原箱 100 盒，原箱尺寸未知。
- 饮料袋 `275 × 180 × 42 mm`，当前商家编码 `6979499760082`。
- 黑茶原箱 72 盒，外尺寸 `420 × 405 × 333 mm`；实际排列为每层 `12×3`、
  共 `2` 层。用户确认黑茶正装大面 `157 × 132 mm`，结合原箱排列得到厚度
  `35 mm`，最终尺寸为 `157 × 132 × 35 mm`。大面属于实物确认，厚度属于已
  确认箱规反推；两个正装口味共用。
- 黑茶体验装 `115 × 61 × 25 mm`，两个体验装口味共用。三盒正装加一个体验装
  的厚度合计 `130 mm`，与黑茶三盒箱实际混装事实一致。
- 悦希单品四盒装纸箱 `200 × 117 × 122 mm`；悦希礼盒三盒装
  `315 × 232 × 285 mm`；悦希彩妆大箱 `158 × 158 × 188 mm`；悦希修颜礼盒
  两套装纸箱 `245 × 230 × 135 mm`。
- 悦希礼盒暂不采集内部容量，使用人工固定包裹配置；修颜礼盒固定每箱一套或两套，
  每套搭配一个修颜礼袋，大于两套时按每两套一箱拆分。
- 三围尺按不规则外形的外包络估算为 `90 × 63 × 23 mm`，商家编码
  `ZZ腰围尺-绿色`；可以参与理论计算，但不能冒充规则长方体实测。
- 酵素4.0体验装 `165 × 70 × 43 mm`，商家编码 `6979499760037`。
- 悦希防晒 `128 × 51 × 36 mm`，商家编码 `6950328271429`。
- 悦希新精油 `107 × 53 × 43 mm`，商家编码 `6975183897416`。
- 悦希保湿面膜 `166 × 117 × 24 mm`，商家编码 `6977235921278`。
- 蛋白粉和酵素3.0产品已经停售，对应三种存量纸箱允许装其他产品；库存耗尽后
  再按用户通知从新推荐候选中停用。这里的“其他产品”仅限KGOS品牌，不含悦希。
- 2026-08-13 补充品牌边界：最初提供的12种普通纸箱均为KGOS箱，不能用于悦希
  护肤品；悦希纸箱也不能反向用于KGOS。品牌不同直接排除，不进入三维摆放搜索。
- 玉米片两个口味原箱固定10包，允许单口味10包或 `5+5` 混装；原箱尺寸暂缺，
  但不影响按确认容量识别。
- 悦希新精油仓库确认小箱最多5瓶。此前外尺寸算法把6～8瓶放进咖啡三盒箱只是
  KGOS箱内的刚性几何结果，品牌与实际容量均不允许。悦希礼盒单盒装扁平箱不作为
  普通单品箱，原因是快递受力面积和支撑力带来的破损风险。
