# 原箱资料补充与拆分确认停点：阶段归档

归档日期：2026-09-17

## 本阶段结论

本轮完成两类收口：装箱实验补齐一批商品与产品原箱资料；审单浮窗把不稳定的“拆分后核验并审核”后半段整体停用。当前多包执行只到填写数量、点击主确定、核对并点击拆分二次确认为止，之后交给人工回ERP核对。

本轮没有操作真实ERP订单。浮窗测试使用隔离状态；装箱实验继续运行在 main 的本地回环服务 `127.0.0.1:3466`。

## 装箱资料

- 新版美式/生椰七条咖啡单盒均为 `90×37×154mm`；80盒原箱为 `393×380×328mm`，`10×4×2` 排列成立。约70盒的最低可发数量仍待实测，正式规则当前只放行完整80盒。
- 红树莓正装即酵素4.0正装，原箱 `385×318×360mm`，沿用已确认15～20盒范围。
- 糖果2.0原箱100盒、酵素4.0体验装原箱72盒、悦希防晒原箱60盒、悦希修颜礼盒原箱20盒已升级为正式原箱；修颜礼盒原箱不含礼袋。
- 黑茶继续使用旧72盒原箱 `420×405×333mm`；新报 `480×322×360mm` 已确认不准确，只保留追溯。
- 印花礼盒空盒原箱 `623×400×420mm`、每箱20个，`2×5×2` 排列成立；它不是玫瑰成品套盒，也不自动包含礼袋。
- 焕颜霜3.0本次原箱记录无效，但商品身份与既有单品尺寸不受影响。
- 其他只有原箱尺寸、缺单品尺寸或实际箱量的资料继续显示为待确认，不把几何估计升级成正式箱规。

## 浮窗拆分临时边界

- 用户可点击“拆分当前订单”；执行保留订单/方案前置检查、唯一目标勾选、混合拆分弹窗、数量填写、添加待拆分、主确定和二次确认。
- 二次确认点击完成后立即返回，不安装或读取拆分网络结果观测器，不读结果行、不滚动、不核验逐包明细、不打开审核菜单、不提交审核。
- 浮窗关闭自动刷新，并在当前窗口内阻止同一系统订单号重复提交拆分；界面明确要求人工回ERP核对。
- 普通单包审核不受本轮影响。
- 旧后半段代码只作为后续重构参考；恢复前必须重新设计状态模型、幂等边界、结果核验与真实订单验收，不能只移除当前停止点。

## 新增交互待办

- 装箱实验添加商品后保留当前商品和搜索内容，支持连续追加同一商品，不再要求每次重新搜索选择。
- 本次商品列表允许随时直接修改数量；修改后校验正整数、使旧结果失效、刷新产品原箱候选，并让下一次计算使用新数量。

## 文档与知识清理

- `CURRENT.md` 继续作为动态实现真值；`tasks/todo.md` 只保留未完成工作。
- 根本规则补齐现役正式原箱、新版七条咖啡尺寸和修颜礼盒“20盒不含礼袋”边界。
- 装箱阶段计划删除“七条咖啡仍缺尺寸”“仍在worktree收口”等过期事实；算法研究明确区分2026-09-12旧基线与当前单箱内核。
- ERP执行规则只描述当前停点；8月拆分后核验归档保留历史事实，不再充当当前入口。
- Claude项目记忆已压缩为当前指针与稳定边界，不再保存290项回归、旧算法下一阶段或“拆分后自动审核”等过期状态。

## 验证

- 装箱尺寸与模拟器专项：`59 passed`。
- 拆分停点、浮窗交互与本地拆分报告专项：`20 passed`。
- 项目完整回归：`327 passed`。
- 测试前后没有新增pytest、Tk浮窗或长期后台进程；既有3466装箱实验服务保持单一监听。

## 下一步入口

1. 装箱页面交互先看 `tasks/todo.md` 的“装箱实验交互待办”。
2. 完整1～N箱算法继续从 `docs/2026-09-12-packing-algorithm-research.md` 与TODO中的 `pack_order()` 接手。
3. 浮窗拆分后核验和审核只从TODO的暂停能力重构项接手，不从8月归档直接恢复。
4. 当前安全边界、资料数量和运行状态统一看 `docs/CURRENT.md`。
