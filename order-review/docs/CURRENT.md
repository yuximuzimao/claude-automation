# 当前状态与接手入口

更新时间：2026-09-09

未标绝对路径的代码路径相对项目根目录。本文只维护当前状态。业务规则见[规则索引](rules/README.md)，操作命令见[使用指南](usage.md)，
完整待办见[任务清单](../tasks/todo.md)。

## 下一步由GPT接手

- 用户已完成本轮方向讨论、资料补充并要求NEAT归档；页面和新算法由后续GPT按用户执行指令开展。
- [装箱实验计划](2026-09-08-packing-simulator-plan.md)仍是活跃待执行材料，未归入已完成历史。
- 首期为资料可查＋KGOS指定单箱实验；之后再多包与盲测，悦希随匹配明细完善。导航骨架已补齐。
- 本轮没有开发页面、服务或ERP写入，也没有修复求解器。装箱底座仍只用于离线计算与审计。

## 当前可用能力及实测边界

- 原订单只读展示、本地方案、精确历史复用、历史包裹模块组合、用户单次触发的单包审核和混合拆分并审核可用。
- 模块重复次数限于同一历史案例已有证据；不把尾包复制成新规则，不记录确定性采用次数。
- 拆分结果已按1→N单向核验；只有短两包路径有用户实测确认，3个以上滚动/虚拟卸载仍待复验。
- 保存方案仍在Tk主线程同步执行持久化，存在数秒等待；后台保存是待办，不是已完成能力。
- 现有案例校验、版本链、备份、恢复、离线回放保持原有边界。本轮未重新做真实案例全量审计。

## 本地资料与实现的区别

- `data/packing-dimensions.json`保留16种现役运输箱与2种待集成新箱，专用原箱单独记录。
- 新版咖啡两款身份、六款悦希尺寸、礼盒制作尺寸与礼袋估计厚度已记录；具体值以资料为准。
- `pendingMappings`中含待编译为实验输入的资料；加载器只把它们当元数据，不表示新品或新箱已被启用。
- `giftPackingFitChecks`保留5组显式布局；输入厚度估计与自动求解结果分开记录。现有求解固定体积排序，会遗漏袋先铺底的解。
- 活动笔记 `data/packing-campaign-notes-2026-09-08.txt`仍未解析；商品匹配后的固定组合定义需要用户逐条确认。
- 商品映射的HEE待补索引在 `../product-mapping/data/products/hee/pending-products.json`，其中焕颜乳编码已有效，不再待更换。

## 验证与工作区边界

- 2026-08-29完整回归历史基线294项；本轮尺寸/审计相关验证51项，不宣称重新执行完整回归。
- 礼盒布局已逐件读回核对数量、朝向、边界、支撑和余量；印花数据已采用制作尺寸。
- 工作区仍有此前未提交的审单实现与跨项目修改。后续GPT先读Git状态；不要重置、覆盖或整批提交这些内容。
- 本轮资料文件默认只保存在本地；脱离本工作区交接时需一并携带资料和原文，不能只依赖远端代码。
- 本阶段过程、资料校验摘要与审计清单见[NEAT归档](archive/2026-09-09-packing-simulator-planning/neat-handoff.md)。
